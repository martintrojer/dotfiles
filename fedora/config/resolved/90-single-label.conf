[Resolve]
# Send single-label A/AAAA queries (bubba, pizero2) to the LAN DNS server.
# systemd-resolved drops them by default; the local Pi-hole is authoritative
# for bare hostnames, so without this they never leave the stub resolver.
ResolveUnicastSingleLabel=yes
