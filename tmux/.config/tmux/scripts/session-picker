#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "$0")" && pwd)"
script="$script_dir/tms"

choice="$($script picker)" || exit 0
[ -n "$choice" ] || exit 0

TMS_SELECTION="$choice" exec "$script" connect-selection
