[sftp.bubba]
host = "bubba"
user = "martintrojer"
port = 22
# Authenticate via the ssh-agent ($SSH_AUTH_SOCK); the RSA key loaded there
# is what actually authorizes against bubba.

[sftp.pizero2]
host = "pzero2"
user = "martintrojer"
port = 22
# Authenticate via the ssh-agent ($SSH_AUTH_SOCK).
